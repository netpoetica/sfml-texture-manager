This is a simple texture manager I created using SFML 2.0 and the STL map class

It requires SFML 2.0, which is available at http://www.sfml-dev.org/

To load a texture:
TextureManager::getTexture(<filename>);